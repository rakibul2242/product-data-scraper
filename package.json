{
  "name": "bauhaus-scraper",
  "version": "2.1.0",
  "description": "Reads product URLs directly from a Google Sheet, scrapes bauhaus.es using a headless browser, writes results back into the same sheet",
  "type": "module",
  "main": "scraper.js",
  "scripts": {
    "start": "node scraper.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^17.4.2",
    "googleapis": "^140.0.1",
    "p-limit": "^5.0.0",
    "puppeteer": "^23.11.1",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
